apiVersion: v1
kind: Service
metadata:
  name: {{ include "lmtp-gateway.fullname" . }}
  labels:
    {{- include "lmtp-gateway.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: lmtp
      protocol: TCP
      name: lmtp
  selector:
    {{- include "lmtp-gateway.selectorLabels" . | nindent 4 }}
