apiVersion: v1
kind: Service
metadata:
  name: {{ include "search-gateway.fullname" . }}
  labels:
    {{- include "search-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "search-gateway.selectorLabels" . | nindent 4 }}
