apiVersion: v1
kind: Service
metadata:
  name: {{ include "antiabuse.fullname" . }}
  labels:
    {{- include "antiabuse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "antiabuse.selectorLabels" . | nindent 4 }}
